# Интеграция Auto Drive - Инструкция

Краткое руководство по настройке Auto Drive для децентрализованного хранения чатов.

## Что такое Auto Drive?

Auto Drive - это децентрализованное хранилище данных от Autonomys Network (DSN). Предоставляет постоянное хранение данных на блокчейне с использованием контент-адресации (CID).

## ⚠️ Важная информация о сетях

**Auto Drive в настоящее время поддерживает ТОЛЬКО mainnet:**

- Тестовая сеть Taurus была закрыта 12 сентября 2025
- Тестовая сеть Chronos пока не поддерживается Auto Drive API
- Все данные чатов сохраняются в mainnet независимо от настройки `AUTONOMYS_NETWORK`

## Быстрая настройка

### 1. Получите бесплатный API ключ

1. Перейдите на **https://ai3.storage/**
2. Войдите через Google, Discord, GitHub или кошелек
3. В левом меню выберите **"developers"**
4. Нажмите **"Create API Key"**
5. Скопируйте ваш API ключ

**Бесплатный тариф:** 20MB/месяц

### 2. Настройте переменные окружения

Добавьте в файл `.env.local`:

```bash
NEXT_PUBLIC_AUTONOMYS_API_KEY=ваш_api_ключ
```

### 3. Готово!

Пакеты уже установлены, интеграция готова к использованию.

## Как это работает

### Автоматическое сохранение

1. **Локальное хранение:** Чаты сразу сохраняются в localStorage браузера
2. **Отложенная загрузка:** Через 5 секунд после последнего изменения данные загружаются в Auto Drive
3. **Фоновая синхронизация:** Каждые 60 секунд все измененные чаты синхронизируются с DSN
4. **Отслеживание CID:** Каждая загрузка получает уникальный CID (Content Identifier)

### Структура файлов

```
lib/
  ├── auto-drive.ts          # Основная интеграция с Auto Drive
  └── chat-persistence.ts    # Логика сохранения чатов
```

## Основные функции

### Ручная загрузка в DSN

```typescript
import { saveChatToDSN } from '@/lib/chat-persistence';

const cid = await saveChatToDSN('chat-123', '0x...');
console.log('CID:', cid);
```

### Загрузка из DSN

```typescript
import { loadChatFromDSN } from '@/lib/chat-persistence';

const chat = await loadChatFromDSN('bafk...');
```

### Проверка статуса

```typescript
import { getAutoDriveStatus } from '@/lib/auto-drive';

const status = await getAutoDriveStatus();
// { connected: true, network: 'mainnet', apiKeyConfigured: true }
```

## Возможности

### ✅ Реализовано

- Автоматическое сохранение с задержкой
- Загрузка/скачивание с повторными попытками
- Отслеживание CID
- Поддержка шифрования (при наличии адреса кошелька)
- Обработка ошибок и уведомления
- Статистика хранилища
- Фоновая синхронизация
- Двойное хранение (локально + DSN)

### 🚧 Ограничения

- Auto Drive поддерживает только mainnet (нет тестовой сети)
- Удаление файлов не поддерживается (неизменяемость блокчейна)
- Квота 20MB/месяц на бесплатном тарифе

## Конфигурация

Настройки автосохранения находятся в `lib/chat-persistence.ts`:

```typescript
const AUTO_SAVE_CONFIG = {
  enabled: true,
  debounceMs: 5000,        // Сохранение через 5 секунд после изменения
  syncIntervalMs: 60000,   // Синхронизация каждую минуту
};
```

## Панель управления

На **https://ai3.storage/** вы можете:

- Просматривать все загруженные файлы
- Скачивать файлы по CID
- Делиться файлами
- Отслеживать квоты
- Управлять API ключами

## Решение проблем

### Ошибка "AutoDrive not initialized"

**Решение:** Проверьте, что `NEXT_PUBLIC_AUTONOMYS_API_KEY` установлен в `.env.local`

### Ошибка "Failed to upload to DSN"

**Решения:**
1. Проверьте правильность API ключа
2. Убедитесь, что не превышена квота 20MB/месяц
3. Проверьте размер файла
4. Проверьте подключение к интернету

## Безопасность

### Шифрование

При указании адреса кошелька данные автоматически шифруются:

```typescript
await saveChatToDSN(chatId, userAddress); // Зашифровано
await saveChatToDSN(chatId);              // Не зашифровано
```

### Приватность

- По умолчанию файлы в Auto Drive **публичные** (если не зашифрованы)
- CID детерминированы (одинаковый контент = одинаковый CID)
- Всегда шифруйте конфиденциальные данные перед загрузкой
- Следите за использованием квоты

## Информация о сети

### Mainnet (Продакшен)
- **Сеть:** `NetworkId.MAINNET`
- **API:** `https://mainnet.auto-drive.autonomys.xyz/api`
- **Токен:** AI3
- **Панель:** https://ai3.storage/

### Тестовые сети
- Taurus: Закрыта с 12 сентября 2025
- Chronos: Пока не поддерживается Auto Drive
- Используйте mainnet для всех операций

## Поддержка

- **Документация:** https://docs.autonomys.xyz/
- **Документация для разработчиков:** https://develop.autonomys.xyz/sdk/auto-drive/overview_setup
- **Панель управления:** https://ai3.storage/
- **GitHub:** https://github.com/autonomys/auto-sdk

## Версии

- **@autonomys/auto-drive:** ^1.6.0
- **@autonomys/auto-utils:** ^1.6.0
- **Поддерживаемые сети:** Только mainnet
- **Обновлено:** Январь 2025

---

Полная документация на английском: [AUTO_DRIVE_SETUP.md](./AUTO_DRIVE_SETUP.md)
